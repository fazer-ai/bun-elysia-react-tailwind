# bun-elysia-react-tailwind

Create a new repo using this template, and run the following commands to get started:

```bash
bun install
bun setup

# Start the development server
bun dev
```

Then, access the app at `http://localhost:3000`.
